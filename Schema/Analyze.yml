info:
  name: Analyze
  type: http
  seq: 1

http:
  method: GET
  url: "{{controllerProtocol}}{{controllerIp}}{{analyzePath}}/openAPISpec"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
