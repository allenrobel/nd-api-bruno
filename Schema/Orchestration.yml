info:
  name: Orchestration
  type: http
  seq: 5

http:
  method: GET
  url: "{{controllerProtocol}}{{controllerIp}}{{msoPath}}/openAPISpec"
  auth: inherit

runtime:
  variables:
    - name: msoPath
      value: /api/v1/mso

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
