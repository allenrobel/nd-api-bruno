info:
  name: Cluster syscfg
  type: http
  seq: 2

http:
  method: GET
  url: "{{controllerProtocol}}{{controllerIp}}/v2/bootstrap/syscfg"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: "200"
    description: Success
    request:
      url: "{{controllerProtocol}}{{controllerIp}}/v2/bootstrap/syscfg"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
