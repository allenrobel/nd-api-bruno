info:
  name: Cluster
  type: http
  seq: 3

http:
  method: GET
  url: "{{controllerProtocol}}{{controllerIp}}/v2/bootstrap/cluster"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: "200"
    description: Success
    request:
      url: "{{controllerProtocol}}{{controllerIp}}/v2/bootstrap/cluster"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
